(ns kinematic.core-test
  (:require kinematic.core
            kinematic.dsl
            kinematic.servers
            kinematic.session
            kinematic.bindings)

  (:use clojure.test))


(deftest example-test
  (is true))